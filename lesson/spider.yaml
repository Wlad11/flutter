# Generated by Spider

# Generates unit tests to verify that the assets exists in assets directory
# Создает модульные тесты для проверки наличия ресурсов в каталоге ресурсов 
# Заебет поставь false
generate_tests: true

# Use this to remove vcs noise created by the `generated` comments in dart code
# Используйте это для удаления шума vcs, создаваемого комментариями `сгенерированные` в коде дротика
no_comments: true

# Exports all the generated file as the one library
export: true

# This allows you to import all the generated references with 1 single import!
use_part_of: true

# Location where all the generated references will be stored
# Место, где будут храниться все сгенерированные ссылки
package: resources

groups:
  - path: images 
    class_name: AppImages
    types: [ .png, .jpg, .jpeg, .webp, .webm, .bmp ]
#  - path: assets/vectors
#    class_name: Svgs
#    package: res
#    types: [ .svg ]